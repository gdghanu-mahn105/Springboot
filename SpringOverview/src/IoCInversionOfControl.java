public class IoCInversionOfControl {
    /*
    Inversion of control: là nguyên lí trong đó quyền kiểm soát việc tạo và quản lí đối tượng
    không nằm ở chính lớp sử dụng, mà được chuyển sang một phần khác
    - khi không dùng IoC: class tự tạo và quản lí các dependencies
    (chặt chẽ, khó test, khó bảo trì)
    - dùng IoC: class nhận dependencies từ bên ngoài, không cần tự tạo
    ( dễ thay đổi, dễ test, linh hoạt, dễ bảo trì, mở rộng )
    IoC: là khái niệm, DI (Dependency injection) là kĩ thuật cụ thể để áp dụng nó.

     */
}
